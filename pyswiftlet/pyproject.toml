[build-system]
requires = ["maturin>=1.12.0,<2.0"]
build-backend = "maturin"

[project]
name = "swiftlet"
version = "0.0.1"
requires-python = ">=3.10"

[tool.maturin]
python-source = "python"
module-name = "swiftlet._core"
